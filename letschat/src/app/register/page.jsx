'use client'

import UserProfile from "@/components/userProfile"; // Make sure to import it with an uppercase

const Register = () => {
    return <UserProfile />; // Use the uppercase name here as well
};

export default Register;
